name: DsNotifier-Nugets

on:
  workflow_dispatch:
  push:
    branches:
      - master

env:
  version: 1.0.${GITHUB_RUN_NUMBER}

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4.0.0
        with:
          dotnet-version: '8.0.x'
          source-url: https://nuget.pkg.github.com/dibrysoft/index.json
        env:
          NUGET_AUTH_TOKEN: ${{secrets.GH_TOKEN}}
          
      - run: dotnet build DsNotifier.Client/DsNotifier.Client.csproj --configuration Release /p:Version=${{ env.version}}
      - name: Create the package
        run: dotnet pack DsNotifier.Client/DsNotifier.Client.csproj --configuration Release /p:Version=${{ env.version }}
      - name: Publish to github
        run: dotnet nuget push DsNotifier.Client/bin/Release/*.nupkg  -k ${{ secrets.GH_TOKEN }} -s https://nuget.pkg.github.com/DibrySoft/index.json --skip-duplicate